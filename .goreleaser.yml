version: 2
before:
  hooks:
    - go mod download
    - go mod tidy
    - go generate ./internal/static/
builds:
  -
    id: po-darwin
    flags:
      - -trimpath
    ldflags:
      - -s -w -X github.com/mkusaka/po/version.Revision={{.ShortCommit}}
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
    goarch:
      - amd64
      - arm64
  -
    id: po-windows
    flags:
      - -trimpath
    ldflags:
      - -s -w -X github.com/mkusaka/po/version.Revision={{.ShortCommit}}
    env:
      - CGO_ENABLED=0
    goos:
      - windows
    goarch:
      - amd64
  -
    id: po-linux
    flags:
      - -trimpath
    ldflags:
      - -s -w -X github.com/mkusaka/po/version.Revision={{.ShortCommit}}
    env:
      - CGO_ENABLED=0
    goos:
      - linux
    goarch:
      - amd64
      - arm64
archives:
  -
    id: po-archive
    name_template: '{{ .ProjectName }}_v{{ .Version }}_{{ .Os }}_{{ .Arch }}{{ if .Arm }}v{{ .Arm }}{{ end }}'
    format_overrides:
      - goos: darwin
        formats:
          - zip
    files:
      - LICENSE
      - CREDITS
      - README.md
      - CHANGELOG.md
checksum:
  name_template: 'checksums.txt'
changelog:
  sort: asc
  filters:
    exclude:
    - '^docs:'
    - '^test:'
nfpms:
  -
    id: po-nfpms
    file_name_template: "{{ .ProjectName }}_{{ .Version }}-1_{{ .Arch }}"
    ids:
      - po-linux
    homepage: https://github.com/mkusaka/po
    maintainer: Ken'ichiro Oyama <k1lowxb@gmail.com>
    description: "po is a Markdown viewer that opens .md files in a browser"
    license: MIT
    formats:
      - deb
      - rpm
      - apk
    bindir: /usr/bin
    epoch: 1
release:
  use_existing_draft: true
